# SDD ledger — plan: docs/superpowers/plans/2026-08-20-vbtech-contact-pipeline.md

## Authority and baseline

- Spec: `docs/superpowers/specs/2026-08-20-vbtech-production-site-design.md` (reachable; binding authority).
- Branch base: `cf3f5a72e79442559bcac6a9ba3ef6ec6f8bdbae` (`origin/main` after README PR #2).
- Worktree: `/Users/thevladbog/PRSOME/v-b/.worktrees/contact-pipeline` on `feat/contact-pipeline`.
- Install: pinned pnpm 11.10.0, 309 packages reused, 0 downloaded after network-enabled reconstruction.
- Baseline unit contracts: content 5, legal 88, web 156; all pass.
- Baseline browser acceptance: 95 pass, 11 expected desktop-only skips outside the sandbox.
- Local container runtime: available (Docker client 29.6.1, Podman-backed server 5.4.2).

## Pre-flight interface and self-consistency scan

| Tasks | Producer / consumer boundary | Finding and ruling |
| --- | --- | --- |
| 1 internal | package manifest, schema, errors, tests, typecheck | The file list omits `tsconfig.json`, but the task requires typecheck. Ruling: add the minimal package tsconfig using the repository pattern; without it the required gate cannot run. Cost if wrong: one extra configuration file. |
| 2 internal | package manifest, templates, previews, tests, build | The file list omits `tsconfig.json`, while typecheck/build are required. Ruling: add the minimum repository-aligned config and scripts needed by those gates. Cost if wrong: small package scaffolding churn. |
| 3 internal | migration, crypto, repository, DB integration tests | The task assumes `@vbtech/contact-function` exists, but no package scaffold is listed and no database launcher exists until Task 7. Ruling: Task 3 owns the minimal package/test scaffold; DB tests accept a bounded test database URL and use a disposable local PostgreSQL 17 instance managed by the controller. Cost if wrong: Task 3 gains support files earlier than listed. |
| 4 internal | config, limiter, captcha, submit, HTTP handler | The Task 4 code snippet puts rate limiting before schema/consent/honeypot, conflicting with spec section 9. Ruling: use the spec order: feature gate; HTTP method/path/content-type/body; origin/host/schema; consent; honeypot; rate limit; captcha; durable enqueue. Cost if wrong: focused tests and implementation differ from the illustrative snippet. |
| 5 internal | Postbox adapter, worker, retry, retention | Self-consistent if repository leases and terminal methods from Task 3 remain explicit interfaces. No ruling needed. |
| 6 internal | web client, Astro form, browser acceptance | The title says “Enable,” but current legal consent is DRAFT and the user requires it to remain unusable. Ruling: implement enabled behavior behind injected/test configuration while production/default builds remain fail-closed and ship no enabled client or captcha request. Cost if wrong: live activation is deferred until legal approval, as required. |
| 7 internal | Compose, E2E, runbook, email review | Public enabled submission cannot legally accept `VBT-PD-02/DRAFT`. Ruling: prove the production gate rejects the draft, and exercise enqueue/delivery E2E through the internal validated pipeline with non-public local test readiness; never present the draft as accepted. Cost if wrong: the E2E does not claim a production-public submission smoke before legal approval. |
| 1 → 2 | `isEmailContact`, locale, validated contact | Task 2 adds a contracts guard not named in Task 1 outputs. Ruling: Task 1 owns and tests `isEmailContact` so Task 2 consumes a stable interface. Cost if wrong: one small interface is delivered one task earlier. |
| 1 → 4 | request schema and public errors | Interfaces align. Task 4 must parse through the shared strict schema before calling external dependencies. |
| 1 → 6 | browser payload and stable error vocabulary | Interfaces align. Web code must not duplicate validation limits or error codes. |
| 2 → 5 | deterministic `RenderedEmail` to Postbox adapter | Interfaces align. Worker consumes rendered output, not raw user fields at the provider boundary. |
| 2 → 7 | RU/EN HTML and text renderings to Mailpit review | Interfaces align. Visual/manual evidence remains separate from snapshot tests. |
| 3 → 4 | transactional `OutboxRepository.accept` | Interfaces align. HTTP success occurs only after durable enqueue. |
| 3 → 5 | leases and terminal methods | Task 3’s interface names terminal methods without signatures. Ruling: define the minimum lease-safe typed methods required by Task 5 and cover their SQL behavior in DB tests. Cost if wrong: later worker work may adjust method shapes with tests. |
| 3 → 7 | PostgreSQL schema and outbox state | Interfaces align. Task 7 uses the same migration and repository, not a parallel schema. |
| 4 → 6 | exact `/api/contact` contract and errors | Interfaces align. Enabled browser acceptance uses controlled local fixtures; default output remains disabled. |
| 4 → 7 | handler acceptance to local pipeline | Draft legality blocks a public enabled handler. Apply the Task 7 ruling above and record both fail-closed public and successful internal delivery evidence. |
| 5 → 7 | worker delivery and retention to Mailpit/E2E | Interfaces align. One notification always; confirmation only for email. |
| 6 → 7 | enabled/disabled UI modes and operational proof | The plan does not require a browser-to-Mailpit combined smoke. Ruling: keep browser acceptance and backend Mailpit E2E independently reproducible; do not weaken either boundary to combine them. Cost if wrong: no single end-to-end browser click until active legal consent exists. |

## Global rulings

- Ruling: keep TypeScript at repository pin 5.9.3, not the plan’s stale 6.0.3 — Astro check currently declares the compatible peer boundary and the user approved this correction — cost if wrong: a future explicit dependency upgrade remains separate.
- Ruling: current `CURRENT_CONTACT_CONSENT_ID` remains `VBT-PD-02/DRAFT`; production submission, captcha loading, and public contact client remain disabled — legal approval and activation are external release gates — cost if wrong: public form availability is deferred.
- Ruling: no Yandex Cloud, Postbox, Timeweb, DNS, deployment, or public-resource mutations occur in this plan execution — only local PostgreSQL 17 and Mailpit are authorized — cost if wrong: live provider verification remains unrun.
- Ruling: the current root `pnpm test` already runs browser acceptance serially, so Task 6 will not run the same browser suite twice; focused enabled/disabled cases may run separately — cost if wrong: equivalent coverage with less duplicate runtime.

## Task progress

Task 1: fix round 1/5 (2 addressed, 0 open — boundary CR/LF rejection; stable consent revision error code; commits `f2b6d78..ca335d6`).
Task 1: complete (commits `cf3f5a7..ca335d6`, review clean).
Task 1: controller verification — exact package test 31/31, typecheck, diff check, and five-package pnpm runtime contract pass; contracts task was uncached and resolved repository-local pnpm 11.10.0.
Task 2: in progress (base `ca335d6`).
Task 2: complete (commits `ca335d6..854d20e`, review clean).
Task 2: ruling — official npm marks `@react-email/components@1.0.12` and its component packages deprecated; implementation uses plain React table markup, `@react-email/render@2.1.0`, and `react-email@6.9.1` preview/export instead — avoids an unsupported runtime dependency — cost if wrong: less component-level convenience, no product behavior loss.
Task 2: deferred acceptance — Mailpit desktop/mobile and client-mode visual inspection remains Task 7; deterministic HTML/text tests and static preview export pass.
Task 3: in progress (base `854d20e`).
Task 3: minor (deferred): plaintext-storage regression test inspects selected columns rather than the exact complete `email_outbox` column set; final review must triage.
Task 3: minor (deferred): durable-hash tests prove message mismatch and captcha/honeypot exclusion but do not mutate every other durable field independently; final review must triage.
Task 3: fix round 1/5 (3 addressed, 1 open — UUID canonicalization, legal consent binding, lease fencing addressed; DB reset bypass open; commits `3fbc3cb..555eba4`).
Task 3: fix round 2/5 (1 addressed, 0 open — every test DROP/TRUNCATE path verifies exact connected DB/role on the same client; commits `555eba4..09cfc71`).
Task 3: complete (commits `854d20e..09cfc71`, review clean; two deferred minors remain for final review).
Task 4: in progress (base `09cfc71`).
Task 4: ruling — current official SmartCaptcha validation endpoint is `https://smartcaptcha.cloud.yandex.ru/validate`, not the stale plan URL; send `secret`, one-time `token`, and structured `requestContext.identity.sourceIp` as `application/x-www-form-urlencoded`, and require `status: ok` plus exact expected host — provider docs updated 2026-08-05 — cost if wrong: provider adapter follows current official contract rather than the outdated plan literal.
Task 4: ruling — official docs recommend treating non-200 as success, but this project’s approved fail-closed security contract overrides that recommendation; timeout, network error, non-200, oversized/malformed JSON, empty/wrong host all map to content-free captcha unavailable/rejected responses and never enqueue — cost if wrong: temporary provider failures reduce availability rather than admitting unverified submissions.
Task 4: ruling — initial local rate limit is 5 attempts per exact 10-minute fixed window for the HMAC-bound source, with rows expiring at the window end; the sixth attempt is rejected and storage failure fails closed — reversible operational default absent from the spec — cost if wrong: limit may need tuning after production telemetry without exposing user content.
Task 4: fix round 1/5 (3 addressed, 0 open — DRAFT route neutrality, immutable production captcha timeout, bounded global rate-limit cleanup; commits `44a8f95..e719832`).
Task 4: complete (commits `09cfc71..e719832`, review clean).
Task 5: in progress (base `e719832`).
Task 5: ruling — use the current official Postbox REST endpoint `POST https://postbox.cloud.yandex.net/v2/email/outbound-emails` with `X-YaCloud-SubjectToken`; production obtains a short-lived service-account IAM token lazily and never stores or logs it — this avoids static provider credentials — cost if wrong: token acquisition remains an injected boundary until live infrastructure is approved.
Task 5: ruling — Postbox `SendEmail` has no documented idempotency key and forbids `Reply-To` in the Simple header list, so the adapter emits a bounded RFC 5322/MIME message through the documented Raw payload, with an outbox-derived deterministic `Message-ID`; fencing prevents competing workers, while a provider-accepted request followed by a lost response remains an explicit at-least-once ambiguity the external API cannot eliminate — cost if wrong: a rare duplicate may still occur after an ambiguous network failure and must be observable without visitor data.
Task 5: ruling — sender, operator notification recipient, and operator reply-to are the legal operator address `hello@v-b.tech`; an email visitor is the notification reply-to and confirmation recipient, while Telegram produces notification only — cost if wrong: operational routing can be changed centrally before activation without changing stored requests.
Task 5: ruling — retry at most 5 delivery attempts with deterministic capped delays of 1 minute, 5 minutes, 15 minutes, and 1 hour; network failures, 429, and 5xx are transient, while documented bounded request/address/content 4xx failures are terminal — reversible operational default absent from the spec — cost if wrong: transient delivery may terminate after the fifth attempt and require metadata-only operational follow-up.
Task 5: ruling — terminal state updates erase ciphertext, IV, and authentication tag atomically; the scheduled retention pass additionally repairs any legacy terminal payload older than 24 hours, deletes terminal metadata older than 30 days in batches of 100, and deletes now-unreferenced request hashes in the same bounded pass — cost if wrong: operational history beyond 30 days is unavailable, while the legal correspondence itself remains in the mailbox under the separately stated retention period.
Task 5: implementer ruling — bound the complete Postbox operation, including success-body consumption, to 10 seconds inside the existing 60-second lease; timeout is transient and the timer remains immutable in production — cost if wrong: slow provider responses are retried rather than holding a lease indefinitely.
Task 5: implementation complete pending independent review — pure 108/108, PostgreSQL 17 integration 30/30, contracts 32/32, email 6/6, root unit 7/7 tasks, root typecheck 8/8 tasks, package-manager contract and diff check pass; no live provider or external resource used.
Task 5: fix round 1/5 (6 reviewer findings plus prepared-token disposal addressed, 0 open — exact Code-only Postbox classification and bounded body disposal, distinct fenced provider-attempt budget, recoverable key/consent/render/token failures, lazy timer token, exact success shape, accurate render telemetry; commits `a48ad71..d2a2389`; pure 127/127, PostgreSQL 17 35/35, root unit 7/7, root typecheck 8/8, package-manager contract and diff check pass).
Task 5: complete (commits `e719832..d2a2389`, scoped re-review clean; live Postbox/IAM/timer/mailbox acceptance remains Task 7/deployment work).
Task 6: in progress (base `d2a2389`).
Task 6: ruling — the default production build must continue emitting no request-capable `/api/contact` or SmartCaptcha client artifact while consent is DRAFT; enabled browser behavior may be exercised only through an explicit test-fixture build boundary that cannot be selected by the public feature flag alone and is never a deployable artifact — cost if wrong: enabled browser acceptance remains isolated from the production build until legal activation.
Task 6: ruling — use the current official on-demand invisible SmartCaptcha client at `https://smartcaptcha.cloud.yandex.ru/captcha.js?render=onload&onload=...`, render with exact site key/locale, keep the service disclosure shield visible, execute only after local validation and consent, and treat load/javascript/token failures as recoverable without submitting — cost if wrong: captcha failure blocks submission rather than bypassing verification.
Task 6: ruling — a request UUID is created once per logical draft, reused after transport/5xx/captcha-unavailable retries where durable acceptance is unknown/absent, and rotated only after an accepted 202 or a deliberate materially changed draft; a consent conflict clears the pending identity and requires a refreshed page/document — cost if wrong: duplicate-safe retries may need server reconciliation rather than silently issuing new identities.
Task 6: ruling — each browser HTTP attempt has an immutable 10-second full-operation timeout, is aborted on form teardown, and freezes all visitor-editable controls while busy; this prevents indefinite busy state and accepted-response data loss without adding automatic retries — cost if wrong: very slow legitimate requests surface as a retry-safe temporary failure.
Task 6: implementation complete pending commit — pure shared-schema client and bounded public response parser; accessible RU/EN state machine; one-time invisible SmartCaptcha; exact consent parity across HTML/client/function/email; explicit guarded loopback fixture in OS temp; default build route-neutral with no request/captcha client artifact.
Task 6: TDD evidence — client RED: missing module, 1 failed suite/0 collected; artifact RED: 1/7 failed on emitted `/api/contact`; browser RED: 44 discovered with captured Desktop 21 failed/1 device skip before fixture/runtime; consent parity RED: stale identity resolved in both email renderers.
Task 6: complete — lint 3/3, typecheck 8/8, root unit 436/436, standard browser 95 pass/11 device skips, enabled browser 51 pass/1 device skip, critical enabled repeat 60/60, build 2/2, package-manager 7/7, diff/artifact scans clean; public flag with DRAFT build-fails closed; no live provider/request/email/cloud/DNS/deploy/GitHub mutation.
Task 6: fix round 1/5 complete (6 Important findings and 1 Minor addressed, 0 open — shared immutable limits, canonical UUID, idempotent abortable lifecycle, in-flight field freeze, shared subscriber-safe captcha loader, deterministic multi-form IDs, visible success reference; commit subject `fix: harden contact client lifecycle`; unit 442/442, standard browser 95 pass/11 device skips, enabled browser 61 pass/1 device skip, critical repeat 66/66, lint 3/3, typecheck 8/8, build 2/2, package-manager 7/7, DRAFT fail-closed and nine-file no-client artifact scans clean; no live external boundary exercised).
Task 6: fix round 2/5 complete (remaining Important addressed — the immutable per-form 10-second signal now bounds shared-loader subscription, token acquisition, and HTTP completion; one timed-out subscriber cannot cancel another, while late callbacks cannot resume the expired attempt; commit subject `fix: bound contact captcha acquisition`; unit 443/443, standard browser 95 pass/11 device skips, enabled browser 65 pass/1 device skip, critical repeat 78/78, lint 3/3, typecheck 8/8, build 2/2, package-manager 7/7, DRAFT fail-closed and nine-file no-client artifact scans clean; no live external boundary exercised).
Task 6: complete (commits `d2a2389..9cf3ee9`, scoped re-review clean; production remains disabled and client-free while consent is DRAFT).
Task 7: in progress (base `9cf3ee9`).
Task 7: ruling — pin local Mailpit to the current official stable release `v1.30.7` (verified from the upstream release marked latest on 2026-08-21), expose PostgreSQL and Mailpit only on loopback, and stop the isolated stack with volumes removed after acceptance unless a documented investigation requires retention — cost if wrong: the local proof is reproducible but intentionally retains no mailbox/database state after completion.
Task 7: TDD RED — configured E2E failed at collection because the deliberately referenced local Mailpit client did not exist; no production/test-helper implementation preceded the failure.
Task 7: complete pending commit — real PostgreSQL 17.10 + Mailpit v1.30.7 E2E 2/2; database regressions 35/35; canonical root 603 pass/12 intentional skips; lint 3/3, typecheck 8/8, build 2/2, package-manager 7/7; DRAFT public build expected-fails and fresh normal output is nine HTML/zero JS; 24 email captures with zero remote requests reviewed; compose containers/volumes/network removed; no live external boundary exercised.
Task 7: complete (commit `93fd4e6`; local operational proof and visual review clean, production/legal/provider gates explicitly unrun).
Task 7: fix round 1/5 complete pending commit (4 Important + 1 Minor addressed — exact marked/tag-scoped Mailpit safety with unrelated-message survival, four-kind visual matrix including EN confirmation, deterministic controlled dark preview, committed 32-capture generator/validator, exact delivery headers; guard 3/3, contact pure 130/130, PG17 35/35, real E2E 3/3, visual 1/1, canonical root 606 pass/12 intentional skips, lint 3/3, typecheck 8/8, build 2/2, package-manager 7/7, DRAFT fail-closed and nine-HTML/zero-JS artifact scan clean; no live external boundary exercised).
Task 7: fix round 2/5 complete pending commit (remaining Important addressed — bounded streamed Mailpit JSON, HTML/text, rendered documents, per-PNG/contact-sheet dimensions and bytes, aggregate evidence, stable manifest measurements, and failure cleanup; focused RED 8/8, GREEN 8/8, mutation 6 expected failures/2 unaffected passes then restored 8/8; contact pure 130/130, PG17 35/35, real E2E 3/3, visual acceptance 9/9, canonical root 606 pass/12 intentional skips, lint 3/3, typecheck 8/8, build 2/2, package-manager 7/7, DRAFT fail-closed and nine-HTML/zero-JS scan clean; regenerated 32-capture sheet reviewed; Compose services/volumes/network removed; no live external boundary exercised).
Final review: fix wave 1 complete pending commit (I1 + M1–M4 addressed — Astro-native dual-gated production runtime and reviewed public site key; guarded loopback/OS-temp production-shaped ACTIVE legal contour using actual layouts/pages; exact active JSON/captcha/single-init browser proof; complete outbox schema classification and six-field mutation coverage; exact evidence prose and activation/rollback runbook; focused web 26/26, active browser 6/6, PG17 30/30 with M1/M2 mutation sensitivity proven, root unit 460/460, full browser 166 pass/12 intentional skips, lint 3/3, typecheck 8/8, build 2/2, package-manager 7/7, public flag+DRAFT exact fail then fresh normal 9 HTML/0 JS-MJS deny-list scan, active 9 HTML/1 JS scan; Compose services/volumes/network removed; production legal DRAFT and all live/provider/deploy/DNS boundaries untouched).
Final review: fix wave 2 complete pending commit (ACTIVE/DRAFT copy contradiction addressed — shared state-aware RU/EN landing content, current-consent instructions/errors, dynamic legal banners/metadata/register, coherent synthetic ACTIVE PD01+PD02 fixture, exact landing plus all-six-legal-route artifact/browser regressions, and approved active UI/legal copy runbook gate; focused content 6/6, focused active artifact 8/8, DRAFT shell/legal 52/52, deliberate draft-phrase reintroduction produced 3 expected failures then restored, root unit 463/463, full browser 168 pass/12 intentional skips, lint 3/3, typecheck 8/8, build 2/2, package-manager 7/7, public flag+DRAFT exact fail then fresh normal 9 HTML/0 JS-MJS zero-hit deny-list scan, ACTIVE 9 HTML/1 shared JS zero-hit phrase/fixture/secret scan; real legal DRAFT, public activation, provider, backend, deploy, DNS, and live resources untouched).
